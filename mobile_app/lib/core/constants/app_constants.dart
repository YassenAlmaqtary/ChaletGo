class AppConstants {
  AppConstants._();

  static const appName = 'ChaletGo';
  static const authTokenKey = 'auth_token';
  static const userDataKey = 'user_data';
  static const refreshTokenKey = 'refresh_token';
  static const requestTimeout = Duration(seconds: 30);
}
